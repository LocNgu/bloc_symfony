{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('build/gallery.css') }}" rel="stylesheet"/>
{% endblock %}

{% block title %}
    bloc - gallery
{% endblock %}

{% block body %}
    <article id="gallery">
    {#    {% for image in images %}#}
    {#        <img class="fade lazy" data-src="{{ image.getImgPath() }}" src="https://via.placeholder.com/1600x900">#}
    {#    {% endfor %}#}
        {% for i in 0..10 %}
            <img class="fade lazy" src="https://via.placeholder.com/1600x900">
        {% endfor %}
    </article>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('lazy') }}

{% endblock %}