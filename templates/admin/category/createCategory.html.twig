{% extends 'admin/admin.html.twig' %}
{% block content %}
    <h1>Category</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_widget(form.save, {'attr': {'class': 'btn'}}) }}
    <a class = "btn cancel" href="{{ path('category') }}">back</a>
    {{ form_end(form) }}
{% endblock %}