{% extends 'admin/admin.html.twig' %}
{% block content %}
<h1>Tags</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_widget(form.save, {'attr': {'class': 'btn'}}) }}
<a class = "btn cancel" href="{{ path('tag') }}">back</a>
{{ form_end(form) }}
{% endblock %}